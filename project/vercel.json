{
  "routes": [
    {
      "src": "/assets/(.*)",
      "dest": "/public/assets/$1"
    }
  ],
  "installCommand": "npm install && curl -fsSL https://deno.land/x/install/install.sh | sh",
  "buildCommand": "export PATH=\"$HOME/.deno/bin:$PATH\" && deno task generate_sitemap && npm run build"
}
